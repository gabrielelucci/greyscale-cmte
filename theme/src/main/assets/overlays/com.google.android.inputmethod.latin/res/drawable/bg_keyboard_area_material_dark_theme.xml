<?xml version="1.0" encoding="utf-8"?>
<layer-list
  xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- ? -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/theme_dashboard_color" />
        </shape>
    </item>
    <!-- That's the actual "one hand dashboard" back ground -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/theme_dashboard_color" />
        </shape>
    </item>
</layer-list>